# Application Settings
JWT_SECRET=super-secret-key-for-poc-change-in-production
DATABASE_URL=sqlite:///./revenue_services.db
ENVIRONMENT=development

# AI & Provider Configuration ('local' or 'cloud')
LLM_PROVIDER=local
SPEECH_PROVIDER=local
OCR_PROVIDER=local
PAYMENT_PROVIDER=mock

# Cloud Credentials (required if LLM_PROVIDER=cloud)
CLOUD_LLM_URL=
CLOUD_LLM_API_KEY=
